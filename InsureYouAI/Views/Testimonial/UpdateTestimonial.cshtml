@model Testimonial
@{
    ViewData["Title"] = "Referans Güncelleme Sayfası";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="card shadow-sm border-0">
    <div class="card-body p-4">
        <div class="d-flex align-items-center mb-4">
            <h5 class="mb-0 fw-bold text-primary">Referans Güncelleme Sayfası</h5>
            <form class="ms-auto position-relative">
                <div class="position-absolute top-50 translate-middle-y search-icon px-3">
                    <i class="bi bi-search text-muted"></i>
                </div>
                <input class="form-control ps-5 rounded-pill" type="text" placeholder="Referans ara...">
            </form>
        </div>
        <hr />
        <form method="post">
            <input type="hidden" asp-for="TestimonialId" />
            <div class="mb-3">
                <label class="form-label fw-semibold">Ad Soyad</label>
                <input type="text" asp-for="NameSurname" class="form-control form-control-lg" placeholder="Örn: Sağlık Sigortası" />
            </div>
            <div class="mb-3">
                <label class="form-label fw-semibold">Yorum Detayı</label>
                <input type="text" asp-for="CommentDetail" class="form-control form-control-lg" placeholder="Örn: Sağlık Sigortası" />
            </div>
            <div class="mb-3">
                <label class="form-label fw-semibold">Başlık</label>
                <input type="text" asp-for="Title" class="form-control form-control-lg" placeholder="Örn: Sağlık Sigortası" />
            </div>
            <div class="mb-3">
                <label class="form-label fw-semibold">Resim Yolu</label>
                <input type="text" asp-for="ImageUrl" class="form-control form-control-lg" placeholder="Örn: Sağlık Sigortası" />
            </div>

            <div class="mt-4">
                <button type="submit" class="btn btn-primary px-4 shadow-sm">
                    <i class="bi bi-plus-circle me-1"></i> Referans Güncelle
                </button>
            </div>
        </form>
    </div>
</div>