<div class="col-12 col-lg-4 col-xl-4 d-flex">
    <div class="card w-100 rounded-4">
        <div class="card-body">
            <div class="d-flex align-items-center mb-3">
                <h6 class="mb-0">Poliçe Portföyü</h6>
            </div>
            <div id="chart2"></div>
        </div>
        <ul class="list-group list-group-flush mb-0 shadow-none">
            <li class="list-group-item bg-transparent border-top"><i class="bi bi-circle-fill me-2 font-weight-bold text-primary"></i> Aktif Olan Poliçeler <span class="float-end">@ViewBag.v1</span></li>
            <li class="list-group-item bg-transparent"><i class="bi bi-circle-fill me-2 font-weight-bold text-orange"></i> Süresi Dolan <span class="float-end">@ViewBag.v2</span></li>
            <li class="list-group-item bg-transparent"><i class="bi bi-circle-fill me-2 font-weight-bold text-info"></i> Bu Ay Başlayan <span class="float-end">@ViewBag.v3</span></li>
        </ul>
    </div>
</div>

<script>
    var total = @ViewBag.v4;
    var active = @ViewBag.v1;
    var expired = @ViewBag.v2;
    var thisMonth = @ViewBag.v3;
</script>