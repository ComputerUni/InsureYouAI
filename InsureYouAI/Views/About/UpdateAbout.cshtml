@model About
@{
    ViewData["Title"] = "Hakkımda Güncelle";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<div class="card shadow-sm border-0">
    <div class="card-body p-4">
        <div class="d-flex align-items-center mb-4">
            <h5 class="mb-0 fw-bold text-primary">Sigorta Hakkımda Düzenleme Sayfası</h5>
        </div>
        <hr />
        <form method="post">
            <input type="hidden" asp-for="AboutId" />
            <div class="mb-3">           
                <label class="form-label fw-semibold">Başlık</label>
                <input type="text" asp-for="Title" class="form-control form-control-lg" />
            </div>
            <div class="mb-3">
                <label class="form-label fw-semibold">İçerik</label>
                <textarea asp-for="Description" rows="5" class="form-control form-control-lg"></textarea>
            </div>
            <div class="mb-3">
                <label class="form-label fw-semibold">Görsel URL</label>
                <input type="text" asp-for="ImageUrl" class="form-control form-control-lg" />
            </div>

            <div class="mt-4">
                <button type="submit" class="btn btn-primary px-4 shadow-sm">
                    <i class="bi bi-plus-circle me-1"></i> Hakkımda Bilgisini Güncelle
                </button>
            </div>
        </form>
    </div>
</div>
