@{
    ViewData["Title"] = "AI ile Yorum Oluştur";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}


<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-robot me-2"></i>
                        AI ile Oluşturulan Yorumlar
                    </h4>
                </div>
                <div class="card-body">
                    @if (ViewBag.testimonials != null)
                    {
                        <ul class="list-unstyled">
                            @foreach (var testimonial in (List<string>)ViewBag.testimonials)
                            {
                                <li class="mb-3 p-3 border rounded">
                                    <i class="bi bi-check-circle-fill text-primary me-2"></i>
                                    @testimonial
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="text-center py-5">
                            <div class="spinner-border text-primary" role="status"></div>
                            <p class="mt-3 text-muted">Metin oluşturuluyor...</p>
                        </div>
                    }
                </div>
                <div class="card-footer text-end">
                    <a asp-action="TestimonialList" class="btn btn-outline-secondary me-2">Geri Dön</a>
                    <a asp-action="CreateTestimonialWithAI" class="btn btn-primary">Yeniden Oluştur</a>
                </div>
            </div>
        </div>
    </div>
</div>